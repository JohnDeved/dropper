{
  "name": "dropper",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently 'npm run rclone' 'npm run server'",
    "server": "tsc && node dist",
    "rclone": "rclone --config='./rclone.conf' mount dropper: ./files",
    "debug": "tsc && node --inspect dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "base64url": "^3.0.1",
    "concurrently": "^5.2.0",
    "express": "^4.17.1",
    "mime-types": "^2.1.27"
  },
  "devDependencies": {
    "@types/express": "^4.17.7",
    "@types/mime-types": "^2.1.0"
  }
}
